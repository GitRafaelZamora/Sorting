#include <stdio.h>
#include <iostream>

using namespace std;


class Sort {
    public: 
        void recursiveInsertionSort(int* arr, int n);
};
    
void Sort::recursiveInsertionSort(int* arr, int n) {
    cout << "Hello From insertion sort." << endl;
}